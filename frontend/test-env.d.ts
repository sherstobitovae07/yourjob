/// <reference types="jest" />
/// <reference types="@testing-library/jest-dom" />

// Этот файл даёт глобальные типы для тестовой среды (jest + jest-dom).
// TypeScript будет подхватывать его автоматически (он входит в include по корню).
