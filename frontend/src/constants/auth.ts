/** Имена куков — должны совпадать с тем, что читает прокси (`app/api/[...proxy]/route.ts`) и axios. */
export const ACCESS_TOKEN_COOKIE = "access_token";
export const TOKEN_TYPE_COOKIE = "token_type";
